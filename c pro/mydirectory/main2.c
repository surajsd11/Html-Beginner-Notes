#include <stdio.h>

int main()
{
    int length, breath,area ;
    printf("enter lenght:");
    scanf("%d",&length);
    printf("enter breath:");
    scanf("%d",&breath);
    area=length*breath;
    printf("area is %d",area);

    
    

    return 0;
}
